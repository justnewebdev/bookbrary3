<h3>Recently Added Book</h3>

<% books.forEach((book, i) => { %>
  <div style="margin-bottom: 10px;">
    <a href="/books/<%= book.id %>"><img src="<%= book.coverImagePath %>" width="100" height="150"></a>
  </div>
<% }) %>
